{
  "name": "sass-landing-page",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:pug": "pug --watch --pretty src/assets/pug/index.pug --out src/",
    "dev:sass": "sass --watch src/assets/sass:src/assets/styles",
    "build": "gulp build",
    "serve": "gulp build && docker compose up -d"
  },
  "keywords": [],
  "author": "Bryan Gutierrez Alvarez <bryguti.alvarez@gmail.com>",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "gulp": "^5.0.0",
    "gulp-cli": "^3.0.0",
    "pug-cli": "^1.0.0-alpha6",
    "sass": "^1.76.0"
  }
}
